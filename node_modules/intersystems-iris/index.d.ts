export declare type GenericValue = string | number | boolean | undefined | null;
export interface IRISQueryResultRow {
    [key: string]: GenericValue;
}
export interface IRISQueryResult {
    rows: IRISQueryResultRow[];
    update?: boolean;
    affected_rows?: number;
}
export declare class IRIS {
    db: any;
    constructor(host: string, port: number, namespace: string, username: string, password: string);
    sql(query: string): Promise<IRISQueryResult>;
    close(): Promise<any>;
}
